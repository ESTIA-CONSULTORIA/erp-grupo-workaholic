{
  "name": "erp-backend",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "prisma generate",
    "build": "npm run prisma:generate && node node_modules/typescript/bin/tsc -p tsconfig.build.json --skipLibCheck --noEmitOnError false || true",
    "start": "npx prisma migrate deploy && node dist/src/main",
    "start:dev": "nest start --watch",
    "start:prod": "npx prisma migrate deploy && node dist/main",
    "prisma:generate": "node node_modules/prisma/build/index.js generate"
  }
}